{
  "name": "card-triage",
  "version": "1.0.0",
  "description": "Swipeable card triage board — mock API and React client",
  "private": true,
  "author": "Sushruth Chandrasekar",
  "license": "MIT",
  "scripts": {
    "api": "node ./server/index.js",
    "start": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend test"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "json-server": "^0.16.1"
  }
}
