import { atom } from "recoil";

export const topCardRefTodosAtom = atom({
  key: "topcardreftodo",
  default: null,
});
export const topCardRefDoneAtom = atom({
  key: "topcardrefdone",
  default: null,
});
