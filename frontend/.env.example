# Origin of the mock card API started by `npm run api` in the repo root.
# Create React App only exposes REACT_APP_* variables to the browser bundle,
# and src/config.js throws at import time if this one is absent.
REACT_APP_API_URL=http://localhost:8080
